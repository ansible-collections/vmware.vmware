- hosts: localhost
  gather_facts: no
  collections:
    - community.general
  vars_files:
    - vars.yml
  tasks:
    - name: Vcsim
      ansible.builtin.import_role:
        name: prepare_vcsim

    - name: Import vmware_folder_template role
      ansible.builtin.import_role:
        name: vmware_folder_template
